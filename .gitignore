# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/

# Publish output
publish/
*.nupkg

# The original Python app, kept beside the repo for parity reference
.FileRouter_python/

# Demo runtime artifacts (generated by DemoReset / a demo run; not source).
# demo/names.txt IS source — the completer seed the demo config reads.
demo/config.json
demo/backups/
demo/failed/
demo/inbox/
demo/invoices/
demo/statements/
demo/deferred/
demo/publish/
demo/history.sqlite
demo/history.sqlite-*
demo/screenshot.png
# scratch route folders from exploratory filing runs
demo/test*/
# Match & Merge scratch: a run renames these in place (some already carry the
# matched id), so they are consumed input rather than a stable fixture
demo/merge-test/

# Scratch audio: source material for the synthesized alert sounds. The ones
# the app actually ships live in src/FileRouter.Wpf/Assets/sounds/.
sounds/

# App runtime files
history.sqlite
history.sqlite-journal
history.sqlite-wal
history.sqlite-shm
crash.log
.env
